name: BIST Taramalari

on:
  schedule:
    - cron: '0 16 * * *' # 1. Tarama (TSİ 19:00)
    - cron: '0 17 * * *' # 2. Tarama (TSİ 20:00)
    - cron: '0 18 * * *' # 3. Tarama (TSİ 21:00)
  workflow_dispatch:

jobs:
  tarama_bir:
    runs-on: ubuntu-latest
    if: github.event.schedule == '0 16 * * *' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v3
      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Calistir
        run: |
          pip install yfinance pandas numpy python-telegram-bot
          python main.py
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          MY_CHAT_ID: ${{ secrets.MY_CHAT_ID }}

  tarama_iki:
    runs-on: ubuntu-latest
    if: github.event.schedule == '0 17 * * *' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v3
      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Calistir
        run: |
          pip install yfinance pandas numpy python-telegram-bot
          python strateji2.py
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          MY_CHAT_ID: ${{ secrets.MY_CHAT_ID }}

  tarama_uc:
    runs-on: ubuntu-latest
    if: github.event.schedule == '0 18 * * *' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v3
      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Calistir
        run: |
          pip install yfinance pandas numpy python-telegram-bot
          python strateji3.py
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          MY_CHAT_ID: ${{ secrets.MY_CHAT_ID }}
